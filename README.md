# Hello World (Android)

Build in the cloud: push this folder to a GitHub repo. The "Build APK" workflow runs
automatically (Actions tab). When it finishes, download "HelloWorld-apk" from the run page.

Build locally: open in Android Studio, or run `gradle assembleDebug`.
Output: app/build/outputs/apk/debug/app-debug.apk
